# getVoteList

Get the vote list of the given account, identified by its address.

## Usage

:::code-group

```ts twoslash [example.ts]
import { publicClient } from "./client";
// [!code focus:99]
const voteList = await publicClient.getVoteList({
  address: "cfx:...",
});
```

```ts [client.ts] filename="client.ts"
// [!include ~/snippets/publicClient.ts]
```

:::

## Returns

[`VoteInfo`](#TODO) []

## Parameters

### address

- **Type:** `Address`

```ts
const voteList = await publicClient.getVoteList({
  address: "cfx:...",
});
```

### epochTag (optional)

- **Type:** `latest_state`| `latest_confirmed`| `latest_checkpoint` | `earliest`
- **Defaut:** `latest_state`

```ts
const voteList = await publicClient.getVoteList({
  address: "cfx:...",
  epochTag: "latest_state", // [!code focus]
});
```

### epochNumber (optional)

- **Type:** `bigint`

```ts
const voteList = await publicClient.getVoteList({
  address: "cfx:...",
  epochNumber: 1n, // [!code focus]
});
```

## JSON-RPC Method

[`cfx_getVoteList`](https://doc.confluxnetwork.org/docs/core/build/json-rpc/cfx-namespace#cfx_getvotelist)
