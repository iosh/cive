# createBlockFilter

Creates a Filter to listen for new block hashes that can be used with [`getFilterChanges`](#TODO).

## Usage

:::code-group

```ts twoslash [example.ts]
import { publicClient } from "./client";

const filter = await publicClient.createBlockFilter(); // [!code focus:99]
// @log: { id: "0x...", type: 'block' }
```

```ts twoslash [client.ts] filename="client.ts"
import { createPublicClient, http } from "cive";
import { mainnet } from "cive/chains";

export const publicClient = createPublicClient({
  chain: mainnet,
  transport: http(),
});
```

:::

## Returns

[`Filter`](#TODO)

## JSON-RPC Methods

[`cfx_newBlockFilter`](https://doc.confluxnetwork.org/docs/core/build/json-rpc/cfx-namespace#cfx_newblockfilter)
