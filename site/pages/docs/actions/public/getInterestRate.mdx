# getInterestRate

Get the interest rate at the given epoch.

## Usage

:::code-group

```ts twoslash [example.ts]
import { publicClient } from "./client";
// [!code focus:99]
const interestRate = await publicClient.getInterestRate({
  epochTag: "latest_state",
});
```

```ts [client.ts] filename="client.ts"
// [!include ~/snippets/publicClient.ts]
```

:::

## Returns

`bigint`

## Parameters

### epochTag (optional)

- **Type:** `latest_state`| `latest_confirmed`| `latest_checkpoint` | `earliest`
- **Defaut:** `latest_state`

```ts
const interestRate = await publicClient.getInterestRate({
  epochTag: "latest_state", // [!code focus]
});
```

### epochNumber (optional)

- **Type:** `bigint`

```ts
const interestRate = await publicClient.getInterestRate({
  epochNumber: 1n, // [!code focus]
});
```

### JSON-RPC Method

[`cfx_getInterestRate`](https://doc.confluxnetwork.org/docs/core/build/json-rpc/cfx-namespace#cfx_getinterestrate)
